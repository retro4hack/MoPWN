<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission 
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:debuggable="false"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_mopwn"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mopwn"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoPWN"
        tools:targetApi="31"
        tools:ignore="HardcodedDebugMode">
        
        <activity android:name=".FileBrowserActivity" android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".InspectPackageActivity" />
        <activity android:name=".ApkDetailsActivity" />
        <activity android:name=".ExportedActivitiesActivity" />
        <activity android:name=".LaunchIntentActivity" />
        <activity android:name=".SourceTreeActivity" />
        <activity android:name=".SourceViewerActivity" />
        <activity android:name=".FileViewerActivity" />
        <activity android:name=".ManifestViewerActivity" />
        <activity android:name=".GeneralToolsActivity" />
        <activity android:name=".FridaManagerActivity" />
        <activity android:name=".SecretsAuditorActivity" />
        
        <service 
            android:name=".ForegroundTrackerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:ignore="ForegroundServicePermission" />
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.mopwn.app.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>
