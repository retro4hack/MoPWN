<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://tools.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_background"
    tools:context=".ManifestViewerActivity"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvManifestTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toStartOf="@id/btnCopyManifest"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvManifestSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="#AAAAAA"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/tvManifestTitle" />

    <LinearLayout
        android:id="@+id/layoutSearchManifest"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvManifestSubtitle">

        <EditText
            android:id="@+id/etManifestSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Find in manifest..."
            android:textColor="@color/white"
            android:textColorHint="#88FFFFFF"
            android:backgroundTint="@color/green_primary"
            android:textSize="14sp"
            android:inputType="text"
            android:maxLines="1" />

        <TextView
            android:id="@+id/tvManifestSearchCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#BBBBBB"
            android:textSize="12sp"
            android:layout_marginHorizontal="8dp"
            android:text="0/0" />

        <ImageButton
            android:id="@+id/btnManifestPrevMatch"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/arrow_up_float"
            android:contentDescription="Previous Match"
            app:tint="@color/green_primary" />

        <ImageButton
            android:id="@+id/btnManifestNextMatch"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/arrow_down_float"
            android:contentDescription="Next Match"
            app:tint="@color/green_primary" />

    </LinearLayout>

    <Button
        android:id="@+id/btnCopyManifest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Copy XML"
        android:textSize="12sp"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:textColor="@color/green_primary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/dividerManifest"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#444444"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/layoutSearchManifest" />

    <ScrollView
        android:id="@+id/scrollViewManifest"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dividerManifest">

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/tvManifestContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#E0E0E0"
                android:textSize="13sp"
                android:typeface="monospace"
                android:textIsSelectable="true"
                android:padding="8dp"
                android:text="Decoding AndroidManifest.xml, please wait..." />

        </HorizontalScrollView>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
